# backend.hcl.example
# Copy to backend.hcl and edit values for your environment.
# IMPORTANT: Do not commit backend.hcl. It is a local-only file.

resource_group_name  = "tfstate-demo-dev-rg"
storage_account_name = "tfstatedemodevsa"
container_name       = "tfstate"
use_azuread_auth     = true

# State file key should be unique per stack.
key = "entra-id/01-users/terraform.tfstate"
