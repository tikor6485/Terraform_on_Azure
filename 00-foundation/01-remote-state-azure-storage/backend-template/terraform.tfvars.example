# Example values only. Copy to terraform.tfvars for local use (do NOT commit terraform.tfvars),
# or set via environment variables (TF_VAR_*).

subscription_id = ""
location        = "northeurope"
environment     = "dev"
resource_prefix = "tf-demo"

tags = {
  environment = "dev"
  managed_by  = "terraform"
}
