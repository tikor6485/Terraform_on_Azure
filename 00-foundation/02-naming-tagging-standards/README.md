# Naming & Tagging Standards (Foundation)

This folder defines reusable naming and tagging conventions for all projects in this repository.
Use the outputs (`name_prefix`, `tags`) as inputs to other stacks/modules.
