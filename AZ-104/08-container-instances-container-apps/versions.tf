// Provider and Terraform version constraints.
// Keep these aligned across stacks for consistent lockfiles and predictable builds.

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.57"
    }
  }
}
